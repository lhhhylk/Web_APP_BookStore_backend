# 价格计算微服务实现总结

## 一、实现概述

已成功实现价格计算函数式微服务，并将其集成到 E-BookStore 系统中。该微服务独立部署，通过 Eureka 服务注册中心进行服务发现，主系统通过 Feign Client 调用该服务来完成订单总价计算功能。

## 二、实现内容

### 2.1 价格计算微服务（price-calculation-service）

**项目位置**: `../price-calculation-service/`

**端口**: 8084

**服务名称**: `price-calculation-service`

#### 主要文件结构

```
price-calculation-service/
├── pom.xml
├── README.md
└── src/main/
    ├── java/com/bookstore/price/
    │   ├── PriceCalculationServiceApplication.java  # 主应用类
    │   ├── controller/
    │   │   └── PriceCalculationController.java      # REST API 控制器
    │   ├── service/
    │   │   └── PriceCalculationService.java         # 价格计算服务
    │   ├── dto/
    │   │   ├── PriceCalculationRequest.java         # 请求 DTO
    │   │   └── PriceCalculationResponse.java        # 响应 DTO
    │   └── exception/
    │       └── GlobalExceptionHandler.java          # 全局异常处理器
    └── resources/
        └── application.yml                          # 配置文件
```

#### API 接口

1. **POST /api/calculate**
   - 功能：计算订单中某种书的总价
   - 请求体：`{"unitPrice": "29.99", "quantity": 3}`
   - 响应：`{"unitPrice": "29.99", "quantity": 3, "totalPrice": "89.97", "success": true, "message": "计算成功"}`

2. **GET /api/calculate**
   - 功能：计算订单中某种书的总价（用于测试）
   - 请求参数：`unitPrice`（单价）、`quantity`（数量）
   - 响应：同上

#### 功能特性

- ✅ 输入：单价（字符串格式）和数量（整数）
- ✅ 输出：总价（保留两位小数，四舍五入）
- ✅ 自动注册到 Eureka 服务注册中心
- ✅ 支持服务发现和负载均衡
- ✅ 完善的错误处理机制
- ✅ 支持 POST 和 GET 两种调用方式

### 2.2 主系统集成

#### 新增文件

1. **PriceCalculationServiceClient.java**
   - 位置：`src/main/java/com/bookstore/bookstore_backend/services/PriceCalculationServiceClient.java`
   - 功能：Feign Client 接口，用于调用价格计算微服务
   - 注解：`@FeignClient(name = "price-calculation-service", path = "/api")`

#### 修改文件

1. **PriceCalculationService.java**
   - 位置：`src/main/java/com/bookstore/bookstore_backend/services/Impl/PriceCalculationService.java`
   - 修改内容：
     - 注入 `PriceCalculationServiceClient`
     - 修改 `calculateLineTotal` 方法，优先使用微服务进行计算
     - 实现降级机制：如果微服务不可用，自动降级到本地计算
     - 保持原有接口不变，确保向后兼容

#### 集成特性

- ✅ 通过 Feign Client 调用微服务
- ✅ 自动服务发现，无需硬编码服务地址
- ✅ 降级机制：微服务不可用时自动降级到本地计算
- ✅ 错误处理：捕获异常并降级
- ✅ 保持原有接口不变，无需修改调用代码

## 三、工作流程

### 3.1 订单创建流程

1. 用户创建订单
2. `OrderService.createOrder()` 被调用
3. 对于订单中的每种书，调用 `priceCalculationService.calculateLineTotal(book.getPrice(), quantity)`
4. `PriceCalculationService` 通过 Feign Client 调用价格计算微服务
5. 价格计算微服务计算总价并返回
6. 主系统使用返回的总价创建订单项
7. 计算所有订单项的总价，得到订单总价

### 3.2 降级流程

1. 如果价格计算微服务不可用（网络问题、服务未启动等）
2. Feign Client 调用失败，抛出异常
3. `PriceCalculationService` 捕获异常
4. 自动降级到本地计算
5. 返回计算结果，确保系统可用性

## 四、部署说明

### 4.1 启动顺序

1. 启动 MySQL 数据库
2. 启动 Redis（如果需要）
3. 启动 Kafka（如果需要）
4. **启动 Eureka Server (8761)**
5. **启动 Price Calculation Service (8084)**
6. 启动 Bookstore Backend (8082)

### 4.2 启动价格计算微服务

```bash
cd ../price-calculation-service
mvn clean install
mvn spring-boot:run
```

### 4.3 验证部署

1. **检查 Eureka 注册**
   - 访问 `http://localhost:8761`
   - 确认 `price-calculation-service` 已注册
   - 状态应为 `UP`

2. **测试微服务 API**
   ```bash
   # POST 方式
   curl -X POST http://localhost:8084/api/calculate \
     -H "Content-Type: application/json" \
     -d '{"unitPrice":"29.99","quantity":3}'
   
   # GET 方式
   curl "http://localhost:8084/api/calculate?unitPrice=29.99&quantity=3"
   ```

3. **测试主系统集成**
   - 启动 Bookstore Backend
   - 创建一个订单
   - 查看订单总价是否正确计算
   - 查看日志确认是否调用了微服务

## 五、技术实现细节

### 5.1 价格计算逻辑

```java
BigDecimal price = new BigDecimal(unitPrice);
BigDecimal quantity = BigDecimal.valueOf(quantity);
BigDecimal total = price.multiply(quantity).setScale(2, RoundingMode.HALF_UP);
```

- 使用 `BigDecimal` 确保精度
- 保留两位小数
- 采用四舍五入

### 5.2 Feign Client 调用

```java
@FeignClient(name = "price-calculation-service", path = "/api")
public interface PriceCalculationServiceClient {
    @PostMapping("/calculate")
    Map<String, Object> calculatePrice(@RequestBody Map<String, Object> request);
}
```

- 使用服务名称 `price-calculation-service` 进行服务发现
- 自动负载均衡（如果有多个实例）
- 支持超时配置

### 5.3 降级机制

```java
if (priceCalculationServiceClient != null) {
    try {
        // 调用微服务
        Map<String, Object> response = priceCalculationServiceClient.calculatePrice(request);
        // 处理响应
    } catch (Exception e) {
        // 降级到本地计算
        return calculateLocally(unitPrice, quantity);
    }
} else {
    // 如果 Feign Client 不可用，使用本地计算
    return calculateLocally(unitPrice, quantity);
}
```

- 优先使用微服务
- 异常时自动降级
- 确保系统可用性

## 六、优势

1. **服务解耦**：价格计算服务独立部署，不影响主系统
2. **可扩展性**：可以轻松扩展多个价格计算服务实例
3. **服务发现**：通过 Eureka 自动发现服务，无需硬编码地址
4. **负载均衡**：Eureka 支持多个实例的负载均衡
5. **高可用**：服务可以独立升级和维护
6. **降级机制**：微服务不可用时自动降级到本地计算
7. **向后兼容**：保持原有接口不变，无需修改调用代码

## 七、测试建议

### 7.1 单元测试

- 测试价格计算逻辑
- 测试错误处理
- 测试边界情况（负数、零、大数等）

### 7.2 集成测试

- 测试微服务 API
- 测试 Feign Client 调用
- 测试降级机制

### 7.3 端到端测试

- 测试订单创建流程
- 测试多种书的价格计算
- 测试微服务不可用时的降级

## 八、后续优化建议

1. **添加缓存**：对常用价格计算结果进行缓存
2. **添加监控**：集成 Micrometer 进行性能监控
3. **添加日志**：完善日志记录，便于排查问题
4. **添加限流**：防止服务被过度调用
5. **添加重试**：Feign Client 添加重试机制
6. **添加断路器**：集成 Hystrix 或 Resilience4j

## 九、总结

本次实现成功创建了价格计算函数式微服务，并将其集成到 E-BookStore 系统中。该微服务：

- ✅ 独立部署，通过 Eureka 进行服务发现
- ✅ 提供 REST API 接口，支持 POST 和 GET 方式
- ✅ 主系统通过 Feign Client 调用，实现服务解耦
- ✅ 具备降级机制，确保系统高可用
- ✅ 保持原有接口不变，向后兼容

系统现在可以支持一个订单中包含多种书，每种书包含一本或多本，所有价格计算都通过微服务完成。

