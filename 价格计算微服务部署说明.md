# 价格计算微服务部署说明

## 一、概述

价格计算微服务是一个独立的函数式服务，用于计算订单中某种书的总价。该服务已集成到 E-BookStore 系统中，主系统通过 Feign Client 调用此微服务来完成订单总价计算功能。

## 二、架构设计

### 2.1 整体架构

```
┌─────────────────┐
│  Eureka Server  │  (端口: 8761) - 服务注册中心
│   (服务发现)     │
└────────┬────────┘
         │
    ┌────┴─────┐
    │          │
┌───▼─────┐  ┌▼──────────────┐
│Price    │  │Bookstore      │
│Calc     │  │Backend        │
│Service  │  │(8082)         │
│(8084)   │  │               │
└─────────┘  └───────────────┘
    │              │
    │              │ (Feign Client调用)
    └──────────────┘
```

### 2.2 组件说明

1. **Eureka Server (8761)**
   - 服务注册中心
   - 提供服务发现和路由功能
   - 所有微服务注册到此处

2. **Price Calculation Service (8084)**
   - 独立的微服务
   - 提供价格计算服务
   - 接口：`POST /api/calculate` 和 `GET /api/calculate?unitPrice={单价}&quantity={数量}`

3. **Bookstore Backend (8082)**
   - 主系统
   - 通过 Feign Client 调用 Price Calculation Service
   - 在订单创建时使用价格计算服务

## 三、实现内容

### 3.1 价格计算微服务

**项目位置**: `../price-calculation-service/`

**主要文件**:
- `PriceCalculationServiceApplication.java` - 主应用类
- `PriceCalculationController.java` - REST API 控制器
- `PriceCalculationService.java` - 价格计算服务实现
- `PriceCalculationRequest.java` - 请求 DTO
- `PriceCalculationResponse.java` - 响应 DTO
- `GlobalExceptionHandler.java` - 全局异常处理器

**API 接口**:
- `POST /api/calculate` - 计算总价（POST方式）
- `GET /api/calculate` - 计算总价（GET方式，用于测试）

### 3.2 主系统集成

**修改的文件**:
- `PriceCalculationServiceClient.java` - 新增 Feign Client 接口
- `PriceCalculationService.java` - 修改实现，通过 Feign Client 调用微服务

**功能特性**:
- 优先使用微服务进行计算
- 如果微服务不可用，自动降级到本地计算
- 支持服务发现，无需硬编码服务地址

## 四、部署步骤

### 4.1 启动顺序

1. 启动 MySQL 数据库
2. 启动 Redis（如果需要）
3. 启动 Kafka（如果需要）
4. **启动 Eureka Server (8761)**
5. **启动 Price Calculation Service (8084)**
6. 启动 Bookstore Backend (8082)

### 4.2 启动价格计算微服务

#### 方式1：使用 Maven

```bash
cd ../price-calculation-service
mvn clean install
mvn spring-boot:run
```

#### 方式2：使用 IDE

1. 导入 `price-calculation-service` 项目
2. 运行 `PriceCalculationServiceApplication.java`

#### 方式3：使用 JAR 文件

```bash
cd ../price-calculation-service
mvn clean package
java -jar target/price-calculation-service-0.0.1-SNAPSHOT.jar
```

### 4.3 验证部署

1. **检查 Eureka 注册**
   - 访问 `http://localhost:8761`
   - 确认 `price-calculation-service` 已注册
   - 状态应为 `UP`

2. **测试微服务 API**
   ```bash
   # POST 方式
   curl -X POST http://localhost:8084/api/calculate \
     -H "Content-Type: application/json" \
     -d '{"unitPrice":"29.99","quantity":3}'
   
   # GET 方式
   curl "http://localhost:8084/api/calculate?unitPrice=29.99&quantity=3"
   ```

3. **测试主系统集成**
   - 启动 Bookstore Backend
   - 创建一个订单
   - 查看订单总价是否正确计算

## 五、使用示例

### 5.1 在主系统中使用

主系统的 `OrderService` 在创建订单时会自动调用价格计算微服务：

```java
// OrderService.java
BigDecimal lineTotal = priceCalculationService.calculateLineTotal(book.getPrice(), requestedQuantity);
```

### 5.2 直接调用微服务

#### POST 请求示例

```bash
curl -X POST http://localhost:8084/api/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "unitPrice": "29.99",
    "quantity": 3
  }'
```

**响应**:
```json
{
  "unitPrice": "29.99",
  "quantity": 3,
  "totalPrice": "89.97",
  "success": true,
  "message": "计算成功"
}
```

#### GET 请求示例

```bash
curl "http://localhost:8084/api/calculate?unitPrice=29.99&quantity=3"
```

**响应**:
```json
{
  "unitPrice": "29.99",
  "quantity": 3,
  "totalPrice": "89.97",
  "success": true,
  "message": "计算成功"
}
```

## 六、功能特性

### 6.1 价格计算逻辑

- 输入：单价（字符串格式）和数量（整数）
- 计算：单价 × 数量
- 输出：总价（保留两位小数，四舍五入）

### 6.2 降级机制

如果价格计算微服务不可用，主系统会自动降级到本地计算，确保系统可用性。

### 6.3 错误处理

- 单价不能为空
- 数量必须大于0
- 单价格式必须正确
- 返回友好的错误信息

## 七、配置说明

### 7.1 价格计算微服务配置

**文件**: `price-calculation-service/src/main/resources/application.yml`

```yaml
spring:
  application:
    name: price-calculation-service
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
```

### 7.2 主系统配置

主系统的 `application.yml` 已经配置了 Eureka 和 Feign，无需额外配置。

## 八、故障排查

### 8.1 微服务无法注册到 Eureka

**问题**: 在 Eureka 控制台看不到 `price-calculation-service`

**解决方案**:
1. 检查 Eureka Server 是否已启动
2. 检查 `application.yml` 中的 Eureka 配置
3. 检查网络连接
4. 查看微服务启动日志

### 8.2 主系统无法调用微服务

**问题**: 创建订单时价格计算失败

**解决方案**:
1. 检查 Price Calculation Service 是否已注册到 Eureka
2. 检查 Bookstore Backend 是否已注册到 Eureka
3. 检查 Feign Client 配置是否正确
4. 查看主系统日志，查看 Feign 调用错误信息
5. 如果微服务不可用，系统会自动降级到本地计算

### 8.3 价格计算结果不正确

**问题**: 计算出的总价不正确

**解决方案**:
1. 检查输入的单价和数量是否正确
2. 检查微服务日志
3. 直接调用微服务 API 进行测试
4. 检查 BigDecimal 精度设置

## 九、扩展说明

### 9.1 负载均衡

Eureka 支持多个服务实例的负载均衡。如果需要多个 Price Calculation Service 实例：

1. 修改 `application.yml` 中的 `server.port`（使用不同的端口）
2. 启动多个实例
3. Eureka 会自动进行负载均衡

### 9.2 添加更多功能

可以在价格计算微服务中添加更多功能，例如：
- 折扣计算
- 税费计算
- 运费计算
- 优惠券计算

## 十、技术栈

- **Spring Boot 3.3.11**
- **Spring Cloud** (服务发现和路由)
- **Eureka Client** (服务注册)
- **OpenFeign** (HTTP 客户端，在主系统中使用)
- **Java 17**
- **Maven** (项目管理)

## 十一、优势

1. **服务解耦**: Price Calculation Service 独立部署，不影响主系统
2. **可扩展性**: 可以轻松扩展多个 Price Calculation Service 实例
3. **服务发现**: 通过 Eureka 自动发现服务，无需硬编码地址
4. **负载均衡**: Eureka 支持多个实例的负载均衡
5. **高可用**: 服务可以独立升级和维护
6. **降级机制**: 微服务不可用时自动降级到本地计算

