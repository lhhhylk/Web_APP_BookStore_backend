# E-BookStore 聊天机器人 Agent 使用指南

## 📋 概述

这是一个基于 deepseek API 和 MCP 服务的聊天机器人 Agent，为 E-BookStore 系统提供智能对话功能。Agent 可以调用 MCP 服务中的工具来查询图书信息。

## 🎯 功能特性

- 🤖 基于 deepseek 大模型的智能对话
- 🔧 集成 MCP 服务作为工具（Tool）
- 🌐 HTTP API 接口
- 🔄 支持工具调用和结果处理
- 🌍 CORS 支持，解决跨域问题

## 📁 项目结构

```
bookstore_Agent_Server/
├── app.py                 # Agent 服务主程序
├── requirements.txt       # Python 依赖包
├── README.md             # 英文说明文档
├── 使用指南.md           # 本文件（中文详细指南）
├── start.bat            # Windows 启动脚本
└── start.sh             # Linux/Mac 启动脚本
```

## 🚀 快速开始

### 步骤1：安装依赖

确保你的系统已安装 Python 3.8 或更高版本，然后安装依赖：

```bash
cd bookstore_Agent_Server
pip install -r requirements.txt
```

或者使用 pip3：

```bash
pip3 install -r requirements.txt
```

### 步骤2：配置 DeepSeek API Key

你需要一个 DeepSeek API Key。获取方式：

1. 访问 [DeepSeek 官网](https://www.deepseek.com/)
2. 注册账号并获取 API Key

配置 API Key 的方式：

#### Windows PowerShell:
```powershell
$env:DEEPSEEK_API_KEY="sk-dc265e34ad6d4a0997280b7bf67d34a9"
$env:PORT="8000"
```

#### Windows CMD:
```cmd
set DEEPSEEK_API_KEY=your_deepseek_api_key_here
set PORT=8000
```

#### Linux/Mac:
```bash
export DEEPSEEK_API_KEY=your_deepseek_api_key_here
export PORT=8000
```

### 步骤3：确保 MCP 服务器配置正确

Agent 服务需要调用 MCP 服务器来查询图书信息。确保：

1. `bookstore_MCP_Server` 目录存在
2. MCP 服务器的数据库配置正确
3. MCP 服务器的依赖已安装

### 步骤4：启动服务

#### Windows:
```bash
start.bat
```

或者直接运行：
```bash
python app.py
```

#### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

或者直接运行：
```bash
python3 app.py
```

服务将在 `http://localhost:8000` 启动。

### 步骤5：测试服务

打开浏览器访问 `http://localhost:8000`，应该看到：

```json
{
  "status": "ok",
  "service": "E-BookStore Chatbot Agent",
  "mcp_tools": 4
}
```

## 🔧 API 接口

### 1. 健康检查

```
GET /
```

返回服务状态。

**响应示例：**
```json
{
  "status": "ok",
  "service": "E-BookStore Chatbot Agent",
  "mcp_tools": 4
}
```

### 2. 聊天接口

```
POST /chat
```

**请求体：**
```json
{
  "messages": [
    {
      "role": "user",
      "content": "帮我找一下关于Python的图书"
    }
  ],
  "stream": false
}
```

**响应：**
```json
{
  "message": "我为您找到了以下关于Python的图书...",
  "tool_calls": [
    {
      "name": "search_books",
      "arguments": "{\"keyword\":\"Python\"}"
    }
  ]
}
```

### 3. 列出工具

```
GET /tools
```

返回所有可用的 MCP 工具。

## 💻 前端集成

前端已经集成了聊天机器人组件，访问路径：`/chatbot`

### 前端配置

前端代码中，Agent API 的 URL 配置在 `bookstore_frontend/src/components/chatbot.jsx`：

```javascript
const AGENT_API_URL = 'http://localhost:8000';
```

如果 Agent 服务运行在不同的端口或地址，请修改此配置。

## 🛠️ 故障排除

### 问题1：无法连接到 DeepSeek API

**症状**: 返回错误 "DeepSeek API错误"

**解决方案**:
1. 检查 `DEEPSEEK_API_KEY` 环境变量是否正确设置
2. 验证 API Key 是否有效
3. 检查网络连接

### 问题2：无法调用 MCP 工具

**症状**: 工具调用失败，返回错误信息

**解决方案**:
1. 确保 `bookstore_MCP_Server` 目录存在
2. 检查 MCP 服务器的数据库配置
3. 确保 MCP 服务器的依赖已安装
4. 查看 Agent 服务的错误日志

### 问题3：跨域问题

**症状**: 前端无法访问 Agent API

**解决方案**:
1. 检查 Agent 服务的 CORS 配置（已在代码中配置为允许所有来源）
2. 确保 Agent 服务正在运行
3. 检查前端代码中的 API URL 是否正确

### 问题4：端口被占用

**症状**: 启动服务时提示端口被占用

**解决方案**:
1. 修改 `PORT` 环境变量使用其他端口
2. 或者修改 `app.py` 中的默认端口

## 📝 使用示例

### Python 示例

```python
import requests

url = "http://localhost:8000/chat"
data = {
    "messages": [
        {"role": "user", "content": "帮我找一下关于Python的图书"}
    ]
}

response = requests.post(url, json=data)
print(response.json())
```

### JavaScript 示例

```javascript
fetch('http://localhost:8000/chat', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    messages: [
      { role: 'user', content: '帮我找一下关于Python的图书' }
    ]
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

### cURL 示例

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"role": "user", "content": "帮我找一下关于Python的图书"}
    ]
  }'
```

## 🔐 安全注意事项

1. **API Key 安全**: 不要在代码中硬编码 API Key，使用环境变量
2. **CORS 配置**: 生产环境应限制 CORS 来源，不要使用 `allow_origins=["*"]`
3. **数据库权限**: 确保 MCP 服务器只授予必要的数据库权限
4. **输入验证**: Agent 服务会对输入进行基本验证，但可以添加更严格的验证

## 📚 集成的 MCP 工具

Agent 集成了以下 MCP 工具：

1. **search_books** - 搜索图书（按书名、作者或标签）
2. **get_book_by_id** - 根据ID获取图书详情
3. **get_all_tags** - 获取所有可用的图书标签
4. **get_books_by_sales** - 获取销量排行榜

## 🔄 工作流程

1. 用户在前端发送消息
2. 前端调用 Agent API (`/chat`)
3. Agent 将消息发送给 DeepSeek API
4. DeepSeek 分析消息，决定是否需要调用工具
5. 如果需要，Agent 调用相应的 MCP 工具
6. MCP 工具查询数据库并返回结果
7. Agent 将工具结果发送给 DeepSeek API
8. DeepSeek 生成最终回复
9. Agent 将回复返回给前端
10. 前端显示回复

## 📄 许可证

本项目为 E-BookStore 系统的一部分。

## 🙏 致谢

感谢使用 E-BookStore 聊天机器人 Agent！如有问题，请参考故障排除部分或联系支持。

