# E-BookStore MCP服务器 - 完整使用指南

## 📋 概述

这是一个为E-BookStore系统开发的MCP（Model Context Protocol）服务器，它允许AI助手（如Cherry Studio中的AI）直接从MySQL数据库查询图书信息。

## 🎯 主要功能

1. **搜索图书** - 根据书名、作者关键词进行模糊搜索
2. **标签筛选** - 按图书标签筛选结果
3. **图书详情** - 根据ID获取完整图书信息（包括库存）
4. **销量排行** - 获取按销量排序的热门图书
5. **标签列表** - 获取所有可用的图书标签

## 📁 项目结构

```
mcp-server/
├── server.py                 # MCP服务器主程序
├── requirements.txt          # Python依赖包
├── README.md                # 英文说明文档
├── 使用指南.md              # 本文件（中文详细指南）
├── CHERRY_STUDIO_CONFIG.md  # Cherry Studio配置指南
├── test_connection.py       # 数据库连接测试工具
├── start.bat               # Windows启动脚本
├── start.sh                # Linux/Mac启动脚本
└── config.json.example     # 配置文件示例
```

## 🚀 快速开始

### 步骤1：安装依赖

确保你的系统已安装Python 3.8或更高版本，然后安装依赖：

```bash
cd mcp-server
pip install -r requirements.txt
```

或者使用pip3：

```bash
pip3 install -r requirements.txt
```

### 步骤2：配置数据库连接

服务器会从环境变量读取数据库配置。你可以通过以下方式设置：

#### Windows PowerShell:
```powershell
$env:DB_HOST="localhost"
$env:DB_PORT="3306"
$env:DB_NAME="bookstore2"
$env:DB_USER="root"
$env:DB_PASSWORD="lhylhylhy050802"
```

#### Windows CMD:
```cmd
set DB_HOST=localhost
set DB_PORT=3306
set DB_NAME=bookstore2
set DB_USER=root
set DB_PASSWORD=lhylhylhy050802
```

#### Linux/Mac:
```bash
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=bookstore2
export DB_USER=root
export DB_PASSWORD=lhylhylhy050802
```

**或者**直接修改 `server.py` 文件中的 `DB_CONFIG` 字典。

### 步骤3：测试连接

运行测试脚本验证数据库连接：

```bash
python test_connection.py
```

如果看到"所有测试通过"，说明配置正确。

### 步骤4：在Cherry Studio中配置

详细步骤请参考 [CHERRY_STUDIO_CONFIG.md](./CHERRY_STUDIO_CONFIG.md)

**简要步骤：**

1. 打开Cherry Studio → 设置 → MCP服务器
2. 添加新服务器：
   - **名称**: `bookstore-mcp`
   - **命令**: `python`
   - **参数**: `C:\Users\96317\Desktop\homework\DA_ER\Web APP\bookstore_backend\mcp-server\server.py`
   - **工作目录**: `C:\Users\96317\Desktop\homework\DA_ER\Web APP\bookstore_backend\mcp-server`
3. 保存并重启Cherry Studio

### 步骤5：使用

在Cherry Studio的对话中，尝试以下命令：

- "帮我找一下关于Python的图书"
- "查找所有标签"
- "显示销量前10的图书"
- "获取ID为1的图书详情"

## 🔧 技术实现详解

### 数据库连接

服务器使用 `mysql-connector-python` 库连接MySQL数据库。连接配置支持：
- 从环境变量读取（推荐）
- 直接在代码中配置

### Tags字段处理

由于JPA的 `@ElementCollection` 注解，tags字段可能以两种方式存储：

1. **books_tags表** - JPA创建的关联表
2. **JSON列** - tags作为JSON数组存储在books表中

服务器会自动检测并适配两种存储方式。

### MCP协议

服务器实现了MCP（Model Context Protocol）标准，通过stdio（标准输入输出）与客户端通信。这允许任何支持MCP的客户端（如Cherry Studio、Claude Desktop等）使用这些工具。

### 工具定义

每个工具都遵循MCP标准：
- `name`: 工具的唯一标识符
- `description`: 工具的描述（AI助手会看到）
- `inputSchema`: JSON Schema定义参数

## 📖 可用工具详解

### 1. search_books - 搜索图书

**功能**: 根据关键词和标签搜索图书

**参数**:
- `keyword` (可选, string): 搜索关键词，匹配书名或作者
- `tag` (可选, string): 图书标签
- `limit` (可选, integer, 默认20): 返回结果的最大数量

**返回**: JSON格式的图书列表，包含：
- `success`: 是否成功
- `count`: 结果数量
- `books`: 图书数组，每本图书包含：
  - `id`: 图书ID
  - `title`: 书名
  - `author`: 作者
  - `tags`: 标签列表
  - `cover`: 封面URL
  - `price`: 价格
  - `description`: 描述
  - `sales`: 销量
  - `inventory`: 库存

**示例使用**:
```
用户: "帮我找一下关于Python的图书"
AI: [调用search_books工具，keyword="Python"]
```

### 2. get_book_by_id - 获取图书详情

**功能**: 根据图书ID获取详细信息

**参数**:
- `book_id` (必需, integer): 图书的唯一标识ID

**返回**: JSON格式的图书详情

**示例使用**:
```
用户: "告诉我ID为1的图书信息"
AI: [调用get_book_by_id工具，book_id=1]
```

### 3. get_all_tags - 获取所有标签

**功能**: 获取数据库中所有可用的图书标签

**参数**: 无

**返回**: JSON格式的标签列表

**示例使用**:
```
用户: "有哪些图书标签？"
AI: [调用get_all_tags工具]
```

### 4. get_books_by_sales - 获取销量排行

**功能**: 获取按销量排序的图书列表

**参数**:
- `limit` (可选, integer, 默认10): 返回结果的最大数量

**返回**: JSON格式的图书列表（按销量降序）

**示例使用**:
```
用户: "显示最热门的10本图书"
AI: [调用get_books_by_sales工具，limit=10]
```

## 🛠️ 故障排除

### 问题1：无法连接到数据库

**症状**: 测试脚本或MCP服务器报错"数据库连接失败"

**解决方案**:
1. 检查MySQL服务是否正在运行
   ```bash
   # Windows
   net start MySQL
   
   # Linux/Mac
   sudo systemctl status mysql
   ```
2. 验证数据库配置信息
   - 主机地址
   - 端口号
   - 数据库名
   - 用户名和密码
3. 测试连接：
   ```bash
   mysql -h localhost -P 3306 -u root -p bookstore2
   ```
4. 检查防火墙设置

### 问题2：找不到Python模块

**症状**: `ModuleNotFoundError: No module named 'mcp'`

**解决方案**:
```bash
pip install -r requirements.txt
```

如果使用Python 3，可能需要：
```bash
pip3 install -r requirements.txt
```

### 问题3：MCP服务器无法启动

**症状**: Cherry Studio中服务器状态显示为"错误"或"未连接"

**解决方案**:
1. 检查Python版本（需要3.8+）:
   ```bash
   python --version
   ```
2. 确认server.py文件路径正确
3. 检查文件权限（Linux/Mac）:
   ```bash
   chmod +x server.py
   ```
4. 手动测试服务器:
   ```bash
   python server.py
   ```
   如果直接运行没有错误输出，说明服务器正常（MCP通过stdio通信）

### 问题3.1：Connection closed 错误

**症状**: 在Cherry Studio中点击启动MCP服务器时出现错误：
```
Error invoking remote method 'mcp:list-tools':
McplErr: MCP error -32000: Connection closed
```

**原因分析**:
这个错误通常表示MCP服务器在启动时崩溃了，可能的原因包括：
1. **缺少依赖包** - 最常见的原因
2. Python路径配置错误
3. server.py文件路径错误
4. 数据库连接失败（在初始化时）
5. 代码语法错误

**解决方案**:

1. **首先运行诊断工具**（推荐）:
   ```bash
   python diagnose_mcp.py
   ```
   这会自动检查所有常见问题。

2. **检查并安装依赖包**:
   ```bash
   pip install -r requirements.txt
   ```
   确保以下包已安装：
   - `mcp` (>=1.0.0)
   - `mysql-connector-python` (>=8.2.0)
   - `pydantic` (>=2.0.0)

3. **验证Python路径**:
   在Cherry Studio配置中，确保：
   - **命令**: `python` 或 `python3`（如果系统PATH中有）
   - 或者使用完整路径: `C:\Python\python.exe`（替换为你的实际路径）

4. **检查server.py路径**:
   - 确保路径正确，注意实际目录名是 `bookstore_MCP_Server`（不是 `bookstore_backend\mcp-server`）
   - 如果路径包含空格，使用引号: `"C:\Users\96317\Desktop\homework\DA_ER\Web APP\bookstore_MCP_Server\server.py"`

5. **检查工作目录**:
   工作目录应该设置为server.py所在的目录:
   ```
   C:\Users\96317\Desktop\homework\DA_ER\Web APP\bookstore_MCP_Server
   ```

6. **测试数据库连接**:
   ```bash
   python test_connection.py
   ```
   如果数据库连接失败，服务器可能在初始化时崩溃。

7. **查看详细错误信息**:
   如果问题仍然存在，可以：
   - 检查Cherry Studio的日志文件
   - 手动运行server.py查看是否有错误输出
   - 运行诊断工具获取详细信息

**快速检查清单**:
- [ ] 已运行 `pip install -r requirements.txt`
- [ ] Python版本 >= 3.8
- [ ] server.py路径正确
- [ ] 工作目录设置正确
- [ ] 数据库连接正常（运行test_connection.py）
- [ ] Cherry Studio配置中的路径使用正确的目录名

### 问题4：路径问题（Windows）

**症状**: 找不到文件或路径错误

**解决方案**:
1. 使用绝对路径
2. 路径中包含空格时使用引号:
   ```
   "C:\Users\96317\Desktop\homework\DA_ER\Web APP\bookstore_backend\mcp-server\server.py"
   ```
3. 使用正斜杠代替反斜杠:
   ```
   C:/Users/96317/Desktop/homework/DA_ER/Web APP/bookstore_backend/mcp-server/server.py
   ```

### 问题5：查询结果为空

**症状**: 工具调用成功但返回空结果

**解决方案**:
1. 检查数据库中是否有数据:
   ```sql
   SELECT COUNT(*) FROM books WHERE deleted = 0;
   ```
2. 检查搜索关键词是否正确
3. 验证标签是否存在:
   ```sql
   SELECT DISTINCT tags FROM books_tags;
   ```

## 🔍 调试技巧

### 1. 使用测试脚本

运行 `test_connection.py` 可以快速诊断问题：
```bash
python test_connection.py
```

### 2. 查看日志

Cherry Studio通常会记录MCP服务器的输出。检查：
- Cherry Studio的日志文件
- 控制台输出（如果从命令行启动）

### 3. 手动测试工具

你可以修改 `server.py` 添加测试代码：

```python
# 在main()函数前添加
async def test():
    result = await search_books("Python", None, 5)
    print(json.dumps(result, ensure_ascii=False, indent=2))

if __name__ == "__main__":
    # 取消注释下面这行来测试
    # asyncio.run(test())
    asyncio.run(main())
```

### 4. 使用MCP Inspector

安装MCP Inspector工具来测试服务器：
```bash
npx @modelcontextprotocol/inspector python server.py
```

## 📚 扩展开发

### 添加新工具

要添加新的工具，需要：

1. 在 `list_tools()` 函数中添加工具定义：
```python
Tool(
    name="new_tool",
    description="新工具的描述",
    inputSchema={
        "type": "object",
        "properties": {
            "param1": {
                "type": "string",
                "description": "参数1的描述"
            }
        }
    }
)
```

2. 在 `call_tool()` 函数中添加处理逻辑：
```python
elif name == "new_tool":
    param1 = arguments.get("param1")
    result = await new_tool_function(param1)
    return [TextContent(type="text", text=json.dumps(result, ensure_ascii=False, indent=2))]
```

3. 实现对应的异步函数：
```python
async def new_tool_function(param1: str) -> dict:
    # 实现逻辑
    return {"success": True, "data": "..."}
```

### 优化查询性能

如果数据库很大，可以考虑：
1. 添加索引（如果还没有）:
   ```sql
   CREATE INDEX idx_title ON books(title);
   CREATE INDEX idx_author ON books(author);
   CREATE INDEX idx_sales ON books(sales);
   ```
2. 实现缓存机制
3. 限制查询结果数量

## 🔐 安全注意事项

1. **密码安全**: 不要在代码中硬编码密码，使用环境变量
2. **数据库权限**: 只授予必要的权限（SELECT）
3. **输入验证**: 虽然已有基本验证，但可以添加更严格的输入检查
4. **SQL注入**: 使用参数化查询（已实现）防止SQL注入

## 📝 更新日志

### v1.0.0 (初始版本)
- 实现基本的图书搜索功能
- 支持按关键词和标签搜索
- 支持获取图书详情
- 支持获取标签列表
- 支持销量排行查询
- 自动适配ElementCollection的两种存储方式

## 🤝 贡献

如果你发现问题或有改进建议，欢迎：
1. 提交Issue
2. 提交Pull Request
3. 联系项目维护者

## 📄 许可证

本项目为E-BookStore系统的一部分。

## 🙏 致谢

感谢使用E-BookStore MCP服务器！如有问题，请参考故障排除部分或联系支持。

