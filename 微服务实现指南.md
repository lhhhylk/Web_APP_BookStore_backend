# E-BookStore 微服务实现指南

## 一、架构设计

### 1.1 整体架构
```
┌─────────────────┐
│  Eureka Server  │  (端口: 8761) - 服务注册中心
│   (服务发现)     │
└────────┬────────┘
         │
    ┌────┴─────┐
    │          │
┌───▼─────┐  ┌▼──────────────┐
│Author   │  │Bookstore      │
│Service  │  │Backend        │
│(8083)   │  │(8082)         │
└─────────┘  └───────────────┘
    │              │
    │              │ (Feign Client调用)
    └──────────────┘
```

### 1.2 组件说明

1. **Eureka Server (8761)**
   - 服务注册中心
   - 提供服务发现和路由功能
   - 所有微服务注册到此处

2. **Author Service (8083)**
   - 独立的微服务
   - 提供根据书名查询作者的服务
   - 接口：`GET /author-service/api/author?title={书名}`

3. **Bookstore Backend (8082)**
   - 主系统
   - 通过 Feign Client 调用 Author Service
   - 提供作者搜索功能给前端

## 二、实现步骤

### 步骤1：在主系统中添加依赖

在主系统的 `pom.xml` 中添加：
- Spring Cloud Starter Eureka Client
- Spring Cloud Starter OpenFeign

### 步骤2：创建 Eureka Server

创建新的 Maven 项目 `eureka-server`：
- 添加 Eureka Server 依赖
- 配置 `application.yml`
- 创建主应用类并启用 Eureka Server

### 步骤3：创建 Author Service 微服务

创建新的 Maven 项目 `author-service`：
- 添加 Eureka Client 依赖
- 创建 AuthorController，提供查询作者接口
- 配置数据库连接（或使用内存数据）
- 注册到 Eureka Server

### 步骤4：在主系统中集成 Feign Client

- 创建 Feign Client 接口
- 配置 Eureka Client
- 在 BookController 中添加作者搜索端点
- 调用 Author Service

## 三、详细实现

### 3.1 主系统配置修改

**修改 `pom.xml`**：添加 Spring Cloud 依赖

**修改 `application.yml`**：
- 添加 Eureka 客户端配置
- 添加 Feign 配置

**创建 `FeignClient`**：
- `AuthorServiceClient.java` - 调用 Author Service 的 Feign 接口

**修改 `BookController`**：
- 添加 `/books/search-author` 端点

### 3.2 Eureka Server 实现

**项目结构**：
```
eureka-server/
├── src/main/java/
│   └── com/bookstore/eureka/
│       └── EurekaServerApplication.java
├── src/main/resources/
│   └── application.yml
└── pom.xml
```

**主要功能**：
- 服务注册中心
- 服务发现
- 健康检查

### 3.3 Author Service 实现

**项目结构**：
```
author-service/
├── src/main/java/
│   └── com/bookstore/author/
│       ├── AuthorServiceApplication.java
│       ├── controller/
│       │   └── AuthorController.java
│       ├── service/
│       │   └── AuthorService.java
│       └── model/
│           └── AuthorResponse.java
├── src/main/resources/
│   └── application.yml
└── pom.xml
```

**API 接口**：
- `GET /api/author?title={书名}` - 根据书名查询作者

## 四、部署说明

### 4.1 启动顺序

1. 启动 MySQL 数据库
2. 启动 Redis
3. 启动 Kafka（如果需要）
4. **启动 Eureka Server (8761)**
5. **启动 Author Service (8083)**
6. 启动 Bookstore Backend (8082)

### 4.2 验证步骤

1. 访问 `http://localhost:8761` 查看 Eureka 控制台
2. 确认 Author Service 已注册
3. 确认 Bookstore Backend 已注册
4. 测试 Author Service API：`http://localhost:8083/api/author?title=测试书名`
5. 测试主系统作者搜索：`http://localhost:8082/books/search-author?title=测试书名`

## 五、技术栈

- **Spring Boot 3.3.11**
- **Spring Cloud** (服务发现和路由)
- **Eureka Server** (服务注册中心)
- **OpenFeign** (HTTP 客户端)
- **MySQL** (数据存储)
- **Maven** (项目管理)

## 六、优势

1. **服务解耦**：Author Service 独立部署，不影响主系统
2. **可扩展性**：可以轻松扩展多个 Author Service 实例
3. **服务发现**：通过 Eureka 自动发现服务，无需硬编码地址
4. **负载均衡**：Eureka 支持多个实例的负载均衡
5. **高可用**：服务可以独立升级和维护

