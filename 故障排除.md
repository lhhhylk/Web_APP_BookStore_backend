# Agent服务故障排除指南

## 常见错误及解决方案

### 错误1: 500 Internal Server Error

**症状**: 前端显示"抱歉，我暂时无法处理您的请求"，服务器日志显示500错误。

**可能原因和解决方案**:

#### 1.1 DeepSeek API Key未设置或无效

**检查方法**:
```bash
# Windows PowerShell
echo $env:DEEPSEEK_API_KEY

# Windows CMD
echo %DEEPSEEK_API_KEY%

# Linux/Mac
echo $DEEPSEEK_API_KEY
```

**解决方案**:
- 确保已设置 `DEEPSEEK_API_KEY` 环境变量
- 验证API Key是否有效（访问DeepSeek官网检查）
- 重新设置环境变量并重启服务

#### 1.2 MCP服务器导入失败

**检查方法**:
运行诊断工具：
```bash
python test_agent.py
```

**解决方案**:
- 确保 `bookstore_MCP_Server` 目录存在
- 确保MCP服务器的依赖已安装：`pip install -r bookstore_MCP_Server/requirements.txt`
- 检查MCP服务器的数据库配置是否正确

#### 1.3 网络连接问题

**检查方法**:
- 检查是否能访问 `https://api.deepseek.com`
- 检查防火墙设置

**解决方案**:
- 确保网络连接正常
- 如果使用代理，配置代理设置

### 错误2: DeepSeek API错误

**症状**: 日志显示 "DeepSeek API错误: 401" 或类似错误。

**可能原因**:
- API Key无效或过期
- API Key权限不足
- API请求格式错误

**解决方案**:
1. 检查API Key是否正确设置
2. 访问DeepSeek官网验证API Key状态
3. 检查API Key是否有足够的余额
4. 查看详细日志了解具体错误信息

### 错误3: MCP工具调用失败

**症状**: 日志显示 "调用MCP工具失败"。

**可能原因**:
- MCP服务器目录不存在
- 数据库连接失败
- MCP服务器函数导入失败

**解决方案**:
1. 运行诊断工具检查MCP服务器：
   ```bash
   python test_agent.py
   ```

2. 检查MCP服务器配置：
   - 确保数据库配置正确
   - 运行 `bookstore_MCP_Server/test_connection.py` 测试数据库连接

3. 检查MCP服务器依赖：
   ```bash
   cd bookstore_MCP_Server
   pip install -r requirements.txt
   ```

### 错误4: 跨域问题

**症状**: 前端无法访问Agent API，浏览器控制台显示CORS错误。

**解决方案**:
- Agent服务已配置CORS，确保服务正在运行
- 检查前端代码中的API URL是否正确
- 检查浏览器控制台的详细错误信息

## 调试步骤

### 步骤1: 运行诊断工具

```bash
cd bookstore_Agent_Server
python test_agent.py
```

这将检查：
- 环境变量配置
- MCP服务器可用性
- MCP函数调用
- DeepSeek API连接

### 步骤2: 查看详细日志

启动Agent服务时，现在会输出详细的日志信息，包括：
- API调用详情
- 工具调用过程
- 错误堆栈信息

查看日志以了解具体错误。

### 步骤3: 测试API端点

使用curl或Postman测试API：

```bash
# 健康检查
curl http://localhost:8000/

# 测试聊天（需要设置正确的消息格式）
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"role": "user", "content": "你好"}
    ]
  }'
```

### 步骤4: 检查MCP服务器

单独测试MCP服务器：

```bash
cd bookstore_MCP_Server
python test_connection.py
```

## 启用调试模式

设置环境变量启用详细调试信息：

```bash
# Windows PowerShell
$env:DEBUG="true"

# Windows CMD
set DEBUG=true

# Linux/Mac
export DEBUG=true
```

然后重启Agent服务，将输出更详细的错误堆栈信息。

## 获取帮助

如果问题仍然存在：

1. 查看Agent服务的日志输出
2. 运行诊断工具 `test_agent.py`
3. 检查MCP服务器的日志
4. 查看浏览器控制台的错误信息
5. 检查网络连接和防火墙设置

## 常见问题快速检查清单

- [ ] DEEPSEEK_API_KEY 已设置且有效
- [ ] MCP服务器目录存在
- [ ] MCP服务器依赖已安装
- [ ] 数据库配置正确
- [ ] 网络连接正常
- [ ] 端口8000未被占用
- [ ] 防火墙允许访问

