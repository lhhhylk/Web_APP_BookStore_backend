# 聊天机器人运行指南

## 📋 快速启动步骤

### 方式1: 使用Python FastAPI服务（当前推荐）

这是已经实现并可以立即运行的方式。

#### 步骤1: 启动Agent服务

打开终端，进入Agent服务目录：

```bash
cd bookstore_Agent_Server
```

**Windows PowerShell:**
```powershell
# 激活虚拟环境（如果使用）
.venv\Scripts\Activate.ps1

# 设置DeepSeek API Key
$env:DEEPSEEK_API_KEY="your_deepseek_api_key_here"

# 启动服务
python app.py
```

**Windows CMD:**
```cmd
# 激活虚拟环境（如果使用）
.venv\Scripts\activate.bat

# 设置DeepSeek API Key
set DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 启动服务
python app.py
```

**Linux/Mac:**
```bash
# 激活虚拟环境（如果使用）
source .venv/bin/activate

# 设置DeepSeek API Key
export DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 启动服务
python app.py
```

服务将在 `http://localhost:8000` 启动。

#### 步骤2: 验证服务运行

打开浏览器访问 `http://localhost:8000`，应该看到：

```json
{
  "status": "ok",
  "service": "E-BookStore Chatbot Agent",
  "mcp_tools": 4
}
```

#### 步骤3: 启动前端

打开新的终端窗口：

```bash
cd bookstore_frontend
npm start
```

前端将在 `http://localhost:3000` 启动。

#### 步骤4: 使用聊天机器人

1. 打开浏览器访问 `http://localhost:3000`
2. 登录系统
3. 在侧边栏菜单中点击 **"智能助手"**
4. 开始与聊天机器人对话

---

## 🔧 详细配置说明

### 1. 环境变量配置

#### DeepSeek API Key

**获取API Key:**
1. 访问 [DeepSeek官网](https://www.deepseek.com/)
2. 注册账号并获取API Key

**设置方式:**

**Windows PowerShell（临时）:**
```powershell
$env:DEEPSEEK_API_KEY="sk-your-api-key-here"
```

**Windows CMD（临时）:**
```cmd
set DEEPSEEK_API_KEY=sk-your-api-key-here
```

**Linux/Mac（临时）:**
```bash
export DEEPSEEK_API_KEY=sk-your-api-key-here
```

**永久设置（Windows）:**
1. 右键"此电脑" → "属性"
2. "高级系统设置" → "环境变量"
3. 添加新的系统变量：
   - 变量名: `DEEPSEEK_API_KEY`
   - 变量值: `sk-your-api-key-here`

**永久设置（Linux/Mac）:**
编辑 `~/.bashrc` 或 `~/.zshrc`:
```bash
export DEEPSEEK_API_KEY=sk-your-api-key-here
```
然后运行: `source ~/.bashrc`

### 2. 数据库配置

确保MCP服务器的数据库配置正确。Agent服务会调用MCP服务器的函数，所以需要确保数据库连接正常。

**检查数据库配置:**
```bash
cd bookstore_MCP_Server
python test_connection.py
```

如果测试失败，检查数据库配置（参考 `bookstore_MCP_Server/使用指南.md`）。

### 3. 端口配置

**Agent服务端口**: 默认8000，可通过环境变量修改：
```bash
export PORT=8000  # Linux/Mac
set PORT=8000     # Windows CMD
$env:PORT=8000    # Windows PowerShell
```

**前端端口**: 默认3000，由React自动管理。

---

## 🧪 测试聊天机器人

### 测试Agent服务API

使用curl测试：

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"role": "user", "content": "你好"}
    ]
  }'
```

或使用Postman：
1. 创建新的POST请求
2. URL: `http://localhost:8000/chat`
3. Headers: `Content-Type: application/json`
4. Body (raw JSON):
```json
{
  "messages": [
    {"role": "user", "content": "帮我找一下关于Python的图书"}
  ]
}
```

### 测试示例问题

在前端聊天界面尝试以下问题：

1. **简单问候**:
   - "你好"
   - "你是谁"

2. **搜索图书**:
   - "帮我找一下关于Python的图书"
   - "查找作者是鲁迅的图书"
   - "搜索标签为'编程'的图书"

3. **获取信息**:
   - "显示销量前10的图书"
   - "获取所有可用的图书标签"
   - "告诉我ID为1的图书信息"

---

## 🛠️ 故障排除

### 问题1: Agent服务无法启动

**症状**: 运行 `python app.py` 时报错

**可能原因和解决方案**:

1. **缺少依赖包**
   ```bash
   pip install -r requirements.txt
   ```

2. **Python版本过低**
   ```bash
   python --version  # 需要 >= 3.8
   ```

3. **端口被占用**
   - 修改PORT环境变量使用其他端口
   - 或关闭占用8000端口的程序

### 问题2: DeepSeek API调用失败

**症状**: 日志显示 "DeepSeek API错误"

**解决方案**:
1. 检查API Key是否正确设置
2. 验证API Key是否有效
3. 检查网络连接
4. 查看详细日志了解具体错误

### 问题3: MCP工具调用失败

**症状**: 日志显示 "调用MCP工具失败"

**解决方案**:
1. 运行诊断工具：
   ```bash
   python test_agent.py
   ```
2. 检查MCP服务器配置
3. 确保数据库连接正常

### 问题4: 前端无法连接Agent服务

**症状**: 前端显示"发送消息失败"

**解决方案**:
1. 确认Agent服务正在运行
2. 检查浏览器控制台的错误信息
3. 确认前端代码中的API URL正确：
   ```javascript
   // bookstore_frontend/src/components/chatbot.jsx
   const AGENT_API_URL = 'http://localhost:8000';
   ```
4. 检查CORS设置（Agent服务已配置允许所有来源）

### 问题5: 聊天机器人无响应

**症状**: 发送消息后没有回复

**解决方案**:
1. 查看Agent服务的日志输出
2. 检查DeepSeek API Key是否有效
3. 检查网络连接
4. 查看浏览器控制台的错误信息

---

## 📊 运行状态检查

### 检查清单

- [ ] Agent服务正在运行（`http://localhost:8000`）
- [ ] 前端服务正在运行（`http://localhost:3000`）
- [ ] DeepSeek API Key已设置
- [ ] 数据库连接正常
- [ ] MCP服务器依赖已安装
- [ ] 浏览器可以访问前端
- [ ] 前端可以连接到Agent服务

### 验证命令

```bash
# 检查Agent服务
curl http://localhost:8000/

# 检查MCP工具端点
curl -X POST http://localhost:8000/mcp-tool \
  -H "Content-Type: application/json" \
  -d '{"tool_name": "get_all_tags", "arguments": {}}'

# 运行诊断工具
python test_agent.py
```

---

## 🚀 启动脚本

### Windows启动脚本

创建 `start_agent.bat`:

```batch
@echo off
echo 启动E-BookStore聊天机器人Agent服务...
echo.

REM 检查环境变量
if "%DEEPSEEK_API_KEY%"=="" (
    echo 错误: DEEPSEEK_API_KEY 环境变量未设置
    echo 请先设置 DEEPSEEK_API_KEY 环境变量
    pause
    exit /b 1
)

REM 激活虚拟环境（如果存在）
if exist "venv\Scripts\activate.bat" (
    call venv\Scripts\activate.bat
)

REM 启动服务
python app.py

pause
```

### Linux/Mac启动脚本

创建 `start_agent.sh`:

```bash
#!/bin/bash

echo "启动E-BookStore聊天机器人Agent服务..."
echo ""

# 检查环境变量
if [ -z "$DEEPSEEK_API_KEY" ]; then
    echo "错误: DEEPSEEK_API_KEY 环境变量未设置"
    echo "请先设置 DEEPSEEK_API_KEY 环境变量"
    exit 1
fi

# 激活虚拟环境（如果存在）
if [ -f "venv/bin/activate" ]; then
    source venv/bin/activate
fi

# 启动服务
python3 app.py
```

使用方式：
```bash
chmod +x start_agent.sh
./start_agent.sh
```

---

## 📝 运行日志

### 查看日志

Agent服务会在控制台输出详细的日志信息，包括：
- API调用详情
- 工具调用过程
- 错误信息

### 日志级别

- **INFO**: 正常操作信息
- **WARNING**: 警告信息
- **ERROR**: 错误信息

### 启用调试模式

设置环境变量启用更详细的调试信息：

```bash
export DEBUG=true  # Linux/Mac
set DEBUG=true     # Windows CMD
$env:DEBUG="true"  # Windows PowerShell
```

---

## 🎯 完整启动流程示例

### Windows PowerShell完整流程

```powershell
# 1. 进入Agent服务目录
cd bookstore_Agent_Server

# 2. 激活虚拟环境（如果使用）
.venv\Scripts\Activate.ps1

# 3. 设置环境变量
$env:DEEPSEEK_API_KEY="sk-your-api-key-here"

# 4. 安装依赖（如果还没安装）
pip install -r requirements.txt

# 5. 运行诊断工具（可选）
python test_agent.py

# 6. 启动Agent服务
python app.py

# 7. 在新的终端窗口中启动前端
cd ..\bookstore_frontend
npm start
```

### Linux/Mac完整流程

```bash
# 1. 进入Agent服务目录
cd bookstore_Agent_Server

# 2. 激活虚拟环境（如果使用）
source .venv/bin/activate

# 3. 设置环境变量
export DEEPSEEK_API_KEY=sk-your-api-key-here

# 4. 安装依赖（如果还没安装）
pip install -r requirements.txt

# 5. 运行诊断工具（可选）
python test_agent.py

# 6. 启动Agent服务
python app.py

# 7. 在新的终端窗口中启动前端
cd ../bookstore_frontend
npm start
```

---

## ✅ 成功运行的标志

1. **Agent服务**:
   - 控制台显示: `Uvicorn running on http://0.0.0.0:8000`
   - 访问 `http://localhost:8000` 看到健康检查响应

2. **前端服务**:
   - 浏览器自动打开 `http://localhost:3000`
   - 可以正常登录和访问聊天界面

3. **聊天功能**:
   - 可以发送消息
   - 收到AI助手的回复
   - 工具调用正常工作（搜索图书等）

---

**按照以上步骤操作，你的聊天机器人就可以正常运行了！**

