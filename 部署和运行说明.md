# 微服务部署和运行说明

## 一、项目结构

```
bookstore_backend/
├── src/                          # 主系统（Bookstore Backend）
├── eureka-server/                # Eureka服务注册中心
│   ├── src/main/java/com/bookstore/eureka/
│   ├── src/main/resources/
│   └── pom.xml
└── author-service/               # Author Service微服务
    ├── src/main/java/com/bookstore/author/
    ├── src/main/resources/
    └── pom.xml
```

## 二、环境要求

- JDK 17
- Maven 3.6+
- MySQL 8.0+（数据库：bookstore2）
- Redis（可选，用于主系统缓存）

## 三、启动步骤

### 3.1 准备工作

1. **启动MySQL数据库**
   - 确保数据库 `bookstore2` 已创建
   - 确保 `books` 表已存在（Author Service需要访问此表）

2. **启动Redis**（如果主系统需要使用）

3. **启动Kafka**（如果主系统需要使用）

### 3.2 启动服务（按顺序）

#### 步骤1：启动Eureka Server

```bash
cd eureka-server
mvn spring-boot:run
```

或者：

```bash
cd eureka-server
mvn clean package
java -jar target/eureka-server-1.0.0.jar
```

**验证**：访问 http://localhost:8761，应该能看到Eureka控制台。

#### 步骤2：启动Author Service

```bash
cd author-service
mvn spring-boot:run
```

或者：

```bash
cd author-service
mvn clean package
java -jar target/author-service-1.0.0.jar
```

**验证**：
- 在Eureka控制台（http://localhost:8761）中应该能看到 `author-service` 已注册
- 测试API：`http://localhost:8083/api/author?title=测试书名`

#### 步骤3：启动Bookstore Backend（主系统）

```bash
# 在项目根目录
mvn spring-boot:run
```

或者：

```bash
mvn clean package
java -jar target/bookstore_backend-0.0.1-SNAPSHOT.jar
```

**验证**：
- 在Eureka控制台（http://localhost:8761）中应该能看到 `community-service`（主系统）已注册
- 测试作者搜索API：`http://localhost:8082/books/search-author?title=测试书名`

## 四、API测试

### 4.1 Author Service API

**端点**：`GET /api/author?title={书名}`

**示例**：
```bash
curl "http://localhost:8083/api/author?title=测试书名"
```

**响应**：
```json
{
  "title": "测试书名",
  "author": "作者名",
  "message": "查询成功",
  "success": true
}
```

### 4.2 Bookstore Backend API（作者搜索）

**端点**：`GET /books/search-author?title={书名}`

**示例**：
```bash
curl "http://localhost:8082/books/search-author?title=测试书名"
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "title": "测试书名",
    "author": "作者名",
    "message": "查询成功",
    "success": true
  },
  "total": 0
}
```

## 五、服务发现

### 5.1 Eureka控制台

访问 http://localhost:8761 查看所有注册的服务：

- **eureka-server**：服务注册中心（不显示在服务列表中）
- **author-service**：作者查询微服务（端口8083）
- **community-service**：主系统（端口8082）

### 5.2 服务调用流程

```
前端请求
    ↓
Bookstore Backend (8082)
    ↓ (通过Feign Client)
Eureka Server (8761) - 服务发现
    ↓ (返回服务地址)
Author Service (8083)
    ↓ (查询数据库)
MySQL (bookstore2.books表)
    ↓ (返回结果)
Bookstore Backend
    ↓
前端响应
```

## 六、故障排查

### 6.1 Author Service无法注册到Eureka

**问题**：Author Service启动后，在Eureka控制台看不到服务

**解决方案**：
1. 检查Eureka Server是否已启动（http://localhost:8761）
2. 检查 `author-service/src/main/resources/application.yml` 中的Eureka配置
3. 检查Author Service日志，查看是否有连接错误

### 6.2 Bookstore Backend无法调用Author Service

**问题**：调用 `/books/search-author` 返回错误

**解决方案**：
1. 检查Author Service是否已注册到Eureka
2. 检查Bookstore Backend是否已注册到Eureka
3. 检查Feign Client配置是否正确
4. 查看Bookstore Backend日志，查看Feign调用错误信息

### 6.3 Author Service查询不到数据

**问题**：Author Service返回"未找到该书的作者信息"

**解决方案**：
1. 检查数据库连接配置
2. 检查 `books` 表是否存在数据
3. 检查书名是否完全匹配（支持模糊匹配）
4. 检查 `deleted` 字段是否为 `false`

## 七、配置文件说明

### 7.1 Eureka Server配置

**文件**：`eureka-server/src/main/resources/application.yml`

- `server.port`: 8761（Eureka Server端口）
- `eureka.client.register-with-eureka`: false（不向自己注册）
- `eureka.client.fetch-registry`: false（不从自己获取注册信息）

### 7.2 Author Service配置

**文件**：`author-service/src/main/resources/application.yml`

- `server.port`: 8083（Author Service端口）
- `spring.application.name`: author-service（服务名称）
- `eureka.client.service-url.defaultZone`: Eureka Server地址
- 数据库配置：连接主系统的数据库 `bookstore2`

### 7.3 Bookstore Backend配置

**文件**：`src/main/resources/application.yml`

- `server.port`: 8082（主系统端口）
- `eureka.client.service-url.defaultZone`: Eureka Server地址
- `feign.client.config`: Feign客户端超时配置

## 八、扩展说明

### 8.1 添加更多微服务

1. 创建新的Spring Boot项目
2. 添加Eureka Client依赖
3. 配置 `application.yml`，设置服务名称和Eureka Server地址
4. 在主应用类上添加 `@EnableEurekaClient` 注解

### 8.2 负载均衡

Eureka支持多个服务实例的负载均衡。如果需要多个Author Service实例：

1. 修改 `author-service/src/main/resources/application.yml` 中的 `server.port`（使用不同的端口）
2. 启动多个实例
3. Feign Client会自动进行负载均衡

### 8.3 服务降级

如果需要在Author Service不可用时提供降级方案，可以使用Hystrix或Resilience4j（Spring Cloud 2020+推荐使用Resilience4j）。

## 九、技术栈总结

- **Spring Boot 3.3.11**：应用框架
- **Spring Cloud 2023.0.3**：微服务框架
- **Eureka Server**：服务注册中心
- **Eureka Client**：服务注册客户端
- **OpenFeign**：HTTP客户端（服务间调用）
- **Spring Data JPA**：数据访问
- **MySQL**：数据库
- **Maven**：项目管理

## 十、注意事项

1. **启动顺序很重要**：必须先启动Eureka Server，再启动其他服务
2. **数据库配置**：Author Service需要访问主系统的数据库
3. **端口冲突**：确保8761、8082、8083端口未被占用
4. **服务名称**：确保 `spring.application.name` 配置正确，Feign Client通过服务名称调用服务
5. **网络连接**：确保所有服务之间可以相互访问（localhost环境通常没问题）

